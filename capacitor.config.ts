import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'shroomshare-front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
